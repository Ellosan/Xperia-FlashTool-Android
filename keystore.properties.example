# Copy to keystore.properties and fill in to produce signed release builds.
# Without this file the release build is unsigned; debug builds are unaffected.
storeFile=keystore/my-release-key.jks
storePassword=
keyAlias=
keyPassword=
